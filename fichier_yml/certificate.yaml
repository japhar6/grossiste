apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bazariko-tls
  namespace: innov
spec:
  secretName: bazariko-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: bazariko.duckdns.org
  dnsNames:
  - bazariko.duckdns.org
