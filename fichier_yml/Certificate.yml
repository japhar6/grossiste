apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bazariko-tls
  namespace: innov
spec:
  secretName: bazariko-tls
  issuerRef:
    name: lets-encrypt-prod
  commonName: ton-domaine.com
  dnsNames:
    - bazariko.duckdns.org
    - api.bazariko.duckdns.og
